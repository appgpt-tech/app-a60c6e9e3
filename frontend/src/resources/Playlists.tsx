//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const PlaylistsTitle = () => {
  const record = useRecordContext();
  return <span>Playlists {record ? `"${record.name}"` : ''}</span>;
};

export const PlaylistsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="name" />
      <ReferenceField source="creator" reference="Users" />
      <ReferenceField source="tracks" reference="Tracks" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const PlaylistsEdit = () => (
  <Edit title={<PlaylistsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="name" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="creator" reference="Users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="tracks" reference="Tracks" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const PlaylistsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="name" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="creator" reference="Users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="tracks" reference="Tracks" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  <ReferenceInput
    source="creator"
    label="creator"
    reference="Users"
    alwaysOn
  />,
  <ReferenceInput source="tracks" label="tracks" reference="Tracks" alwaysOn />,
];
